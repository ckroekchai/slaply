export default function AdminUnlockPage() {
  return (
    <main className="page-shell">
      <section className="container section-header">
        <span className="eyebrow">Admin fallback</span>
        <h2>Manual unlock</h2>
        <p>
          Internal fallback route for paid customers whose report did not unlock automatically. This must be protected
          before launch.
        </p>
      </section>
    </main>
  );
}
